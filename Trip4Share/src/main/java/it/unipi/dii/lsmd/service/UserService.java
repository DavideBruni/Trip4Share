package it.unipi.dii.lsmd.service;

import it.unipi.dii.lsmd.dto.AuthenticatedUserDTO;
import org.springframework.stereotype.Service;

public interface UserService {

    //AuthenticatedUserDTO authenticate(String username, String password);
    //RegisteredUserDTO registerCustomerAndAuth(CustomerRegistrationDTO customerRegistrationDTO) throws BusinessException;

}
